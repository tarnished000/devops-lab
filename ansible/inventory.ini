[app_servers]
# Replace with a real host once one exists (Terraform stage provisions it), e.g.:
# app01 ansible_host=203.0.113.10 ansible_user=deploy

[app_servers:vars]
ansible_python_interpreter=/usr/bin/python3
