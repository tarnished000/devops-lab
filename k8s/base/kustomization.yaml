apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - postgres-statefulset.yaml
  - postgres-service.yaml
  - app-deployment.yaml
  - app-service.yaml
  - ingress.yaml

# Note: notes-db-secret is intentionally NOT listed here.
# Create it imperatively (see secret.example.yaml) so real credentials
# never end up committed to git.
