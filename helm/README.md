# helm

Helm chart приложения — переход от голых YAML-манифестов (deployment.yaml, service.yaml, ingress.yaml) к параметризованному chart (Chart.yaml, values.yaml, templates/).

**Статус:** ⚪ Запланировано (Неделя 7.5)
