groups:
  - name: notes-app
    rules:
      - alert: AppDown
        expr: up{job="notes-app"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "notes-app is down"
          description: "Prometheus has not been able to scrape notes-app for 1 minute."

      - alert: HighErrorRate
        expr: |
          sum(rate(notes_api_requests_total{status=~"5.."}[5m]))
          / sum(rate(notes_api_requests_total[5m])) > 0.05
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "notes-app 5xx error rate above 5%"
          description: "More than 5% of requests have returned a 5xx status over the last 5 minutes."

      - alert: HighRequestLatency
        expr: |
          histogram_quantile(0.95,
            sum(rate(notes_api_request_duration_seconds_bucket[5m])) by (le)
          ) > 1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "notes-app p95 latency above 1s"
          description: "95th percentile request latency has exceeded 1 second for 5 minutes."
